#pragma once 

#include <cstddef>
#include <cstdint>
#include <sys/types.h>

using size = ssize_t;

using uchar = uint8_t;
using ushort = uint16_t;
using uint = uint32_t;
using ulong = uint64_t;
using usize = size_t;
